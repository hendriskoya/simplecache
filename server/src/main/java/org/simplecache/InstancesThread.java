package org.simplecache;

public class InstancesThread {

    /*private final ConcurrentMap<String, InstanceHandler> servers;

    public InstancesThread() {
        this.servers = new ConcurrentHashMap<>();
    }

    public void add(String id, InstanceHandler instanceHandler) {
        this.servers.put(id, instanceHandler);
    }

    public Collection<InstanceHandler> get() {
        return servers.values();
    }

    public InstanceHandler remove(String id) {
        return servers.remove(id);
    }*/
}
